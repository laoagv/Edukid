window.addEventListener("DOMContentLoaded", event => {
    const activeFormElement = Document.activeElement;
    const clickSignBtn = function () {
        document.location = "sign-up.html"
    }

    signBtn.onclick = clickSignBtn;

})